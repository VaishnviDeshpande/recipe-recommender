Flask==2.3.2
flask-cors==6.0.0            # If your app uses CORS (Cross-Origin Resource Sharing)
gunicorn==21.2.0             # Production server for Flask on Render

numpy==1.24.4                # Essential for ML/data
pandas==2.0.3                # Data manipulation
scikit-learn==1.5.2          # ML models
scipy==1.14.1                # Scientific computations

requests==2.32.3             # HTTP requests (common)
Werkzeug==3.1.3              # Flask dependency (keep compatible version)

tensorflow==2.18.0           # Keep only if your app uses TensorFlow

matplotlib==3.10.0           # Optional, only if you generate plots
seaborn==0.13.2              # Optional, visualization on top of matplotlib
